apiVersion: v1
kind: Service
metadata:
  name: movie-service
spec:
  type: ClusterIP
  selector:
    app: movie-service
  ports:
    - port: {{ .Values.movieService.port }}
      targetPort: {{ .Values.movieService.port }}