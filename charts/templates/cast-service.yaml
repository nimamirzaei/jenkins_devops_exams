apiVersion: v1
kind: Service
metadata:
  name: cast-service
spec:
  type: ClusterIP
  selector:
    app: cast-service
  ports:
    - port: {{ .Values.castService.port }}
      targetPort: {{ .Values.castService.port }}